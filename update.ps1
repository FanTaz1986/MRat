# Save this as git_push.ps1 and run in your project folder

# Stage all changes
git add .

# Commit with a message (edit the message as needed)
git commit -m "Update game code"

# Push to the current branch on origin
git push origin HEAD